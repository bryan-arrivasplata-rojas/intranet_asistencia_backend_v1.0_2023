// Ocultar el loader cuando el contenido se haya cargado
window.addEventListener('load', function() {
    var loaderOverlay = document.getElementById('loader-overlay');
    loaderOverlay.style.display = 'none';
});